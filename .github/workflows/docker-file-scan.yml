name: Docker file scan

on:
    workflow_call:
    
jobs:
    scan:
        runs-on: ubuntu-latest
        steps:
            - name: code checkout
              uses: actions/checkout@v4 
              