name: End to End DevSecOps Pipeline

on:
  push:
    branches: [dev]

jobs: 
  check-code:
    uses: ./.github/workflows/check-code-linter.yml
    strategy:
      matrix:
        service-path:
          - backend/auth-service
          - backend/user-service
          - backend/product-service
          - backend/cart-service
          - backend/order-service
          - backend/api-gateway
        #   - frontend/client-app
    with:
      service-path: ${{ matrix.service-path }}
    secrets: inherit

#   # -----------------------------
#   # Dependency check for all services
#   # -----------------------------
#   code-dependency:
#     uses: ./.github/workflows/code-dependency.yml
#     strategy:
#       matrix:
#         service-path:
#           - backend/auth-service
#           - backend/user-service
#           - backend/product-service
#           - backend/cart-service
#           - backend/order-service
#           - backend/api-gateway
#           - frontend/client-app
#     with:
#       service-path: ${{ matrix.service-path }}
#     secrets: inherit

#   # -----------------------------
#   # General code quality check (single job)
#   # -----------------------------
#   code-quality:
#     uses: ./.github/workflows/code-quality.yml
#     secrets: inherit

#   # -----------------------------
#   # Dockerfile scan for backend services (matrix)
#   # -----------------------------
#   docker-file:
#     uses: ./.github/workflows/docker-file-scan.yml
#     strategy:
#       matrix:
#         service-path:
#           - backend/auth-service
#           - backend/user-service
#           - backend/product-service
#           - backend/cart-service
#           - backend/order-service
#           - backend/api-gateway
#     with:
#       service-path: ${{ matrix.service-path }}
#     secrets: inherit

#   # -----------------------------
#   # Secrets validation (single job)
#   # -----------------------------
#   secrets:
#     uses: ./.github/workflows/secrets.yml