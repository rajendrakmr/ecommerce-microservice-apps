name: Check Code quality SAST

on:
    workflow_call:

jobs:
    check-sast:
        runs-on: ubuntu-latest
        steps:
            - name: checkout code
              uses: actions/checkout@v4
              